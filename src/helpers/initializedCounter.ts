// import { Counter } from '../app/modules/counter/counter.model';

// export const initializeReferralCounter = async () => {
//   const existing = await Counter.findById('referral');
//   if (!existing) {
//     await Counter.create({
//       _id: 'referral',
//       count: 100000,
//     });
//   }
// };
